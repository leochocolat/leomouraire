let controller = new ScrollMagic.Controller();

new ScrollMagic.Scene({
        // triggerElement: ".bloc-content",
        // triggerHook: "0",
        // duration: "100%"
    })
    // .setTween(clip)
    // .addTo(controller);
